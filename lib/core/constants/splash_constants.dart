/// Splash / onboarding timing.
abstract final class SplashConstants {
  /// Minimum time the splash UI stays visible before routing away.
  static const Duration minDisplayDuration = Duration(seconds: 4);
}
